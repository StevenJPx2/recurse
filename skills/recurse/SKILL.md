---
name: recurse
description: Entry point for the recurse RLM runtime. Load the core guide for how to work in a recurse session.
hidden: true
---

# recurse

This is a discovery stub. The real guide always matches the installed version:

- Shell: `recurse skills get core` (add `--full` for the Python API reference).
- Inside the kernel: `print(read_skill("core"))` and
  `print(read_skill("core", "references/python-api.md"))`.

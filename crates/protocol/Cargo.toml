[package]
name = "recurse-protocol"
description = "Wire types for the recurse daemon, kernel, and clients (docs/protocol.md)"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }

[lints]
workspace = true
